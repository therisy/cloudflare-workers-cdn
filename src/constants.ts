const MAX_AGE = 60 * 60 * 24 * 2; // 2 days
const BUCKET = "files";
const SITE_URL = "https://cdn.risy.workers.dev";
const SITE_PATH = "file";
const ONE_MB = 1024 * 1024;
const MAX_FILE_SIZE = ONE_MB;

export {
    MAX_AGE,
    BUCKET,
    SITE_URL,
    SITE_PATH,
    ONE_MB,
    MAX_FILE_SIZE,
}